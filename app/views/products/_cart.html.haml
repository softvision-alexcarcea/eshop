%span.label.label-success
  %i.icon-shopping-cart.icon-white
  Shopping Cart
- if cart and cart.any?
  - total = 0
  %ul.unstyled
    - cart.each do |product, count|
      - total += product.price * count
      %li
        .row
          .span3
            %span.label.label-info= "(#{count})"
            %span.text-info= "#{product.name}"
          .span1
            = link_to remove_product_path(product), :class => "btn-mini btn-danger", :title => "Remove from Cart", :method => :delete, :confirm => "Are you sure you want to remove this item from your cart?" do
              &times;
  %span.text-info= "Total: $%.2f" % total
  %span.pull-right
    %a{ :href => "#", :class => "btn btn-primary" } Buy!
- else
  %br
  %br
  %span.label.label-info 0 items
